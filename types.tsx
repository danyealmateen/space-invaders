export interface PlayerProps {
  initialPosition: {
    x: number;
    y: number;
  };
}

export interface AlienProps {
  initialPosition: {
    x: number;
    y: number;
  };
}

export interface ProjectileProps {
  initialPosition: {
    x: number;
    y: number;
  };
}
